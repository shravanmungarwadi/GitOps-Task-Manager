# Kubernetes Raw Manifests (Reference Only)

These are the original raw Kubernetes YAML manifests written in Phase 3 to learn Kubernetes fundamentals — Deployments, Services, ConfigMaps, Secrets, PVCs, Ingress, and HPA.

Active deployments use the **Helm chart** in [`/helm`](../helm). These files are kept as a learning reference.
